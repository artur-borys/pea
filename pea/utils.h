#pragma once
#include <iostream>
#include <fstream>
#include <string>
#include <sstream>
#include <random>
#include <chrono>

using namespace std;

namespace utils{
	int *randomSolution(int size);
	int *ascendingSolution(int size);
}
